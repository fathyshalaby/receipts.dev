# Dependencies
node_modules/

# Receipts output — default is gitignore + CI artefact (open decision §17.3).
# To commit receipts into the branch so reviewers see them in the diff,
# delete this line or force-add: `git add -f .receipts/<id>`.
.receipts/

# Playwright
/test-results/
/playwright-report/
/.playwright/

# Build / editor noise
dist/
*.log
.DS_Store
.env
.env.local
